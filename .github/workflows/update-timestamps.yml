name: Update Timestamps
on:
  push:
    branches: [ main ]

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update timestamps
        run: |
          TIMESTAMP=\$(date -u +%Y-%m-%dT%H:%M:%SZ)
          TODAY=\$(date -u +%Y-%m-%d)
          find faqs help services webpages personnel -name '*.json' 2>/dev/null | while read file; do
            sed -i "s/\\"dateModified\\":.*./\\"dateModified\\": \\"\$TIMESTAMP\\",/" "\$file" 2>/dev/null || true
          done
          [ -f "sitemap.xml" ] && sed -i "s|<lastmod>[0-9]\\{4\\}-[0-9]\\{2\\}-[0-9]\\{2\\}</lastmod>|<lastmod>\$TODAY</lastmod>|g" sitemap.xml
          [ -f "index.html" ] && sed -i "s|Last updated: [0-9]\\{4\\}-[0-9]\\{2\\}-[0-9]\\{2\\}|Last updated: \$TODAY|g" index.html
          git config user.name 'Schema Monster'
          git config user.email 'noreply@schema.monster'
          git add -A
          git diff --staged --quiet || (git commit -m '🔄 Fresh timestamps' && git push)
